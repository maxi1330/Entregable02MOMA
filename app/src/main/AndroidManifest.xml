<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maxig.entregable02android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.BaseDeDatos" />
        <activity android:name=".View.FacebookLoginActivity" />
        <activity android:name=".View.ActivityDetalleObras"
                  android:label="Detalles"
                  android:parentActivityName=".View.MainActivity"/>
        <activity android:name=".View.ActivityPerfil"
                  android:label="Perfil"
                  android:parentActivityName=".View.MainActivity"/>
        <activity android:name=".View.ActivityChat"
                  android:label="Chat"
                  android:parentActivityName=".View.MainActivity"/>
    </application>

</manifest>